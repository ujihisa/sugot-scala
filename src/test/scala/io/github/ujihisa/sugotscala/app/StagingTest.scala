package io.github.ujihisa.sugotscala.app

import org.scalatest._

class StagingTest extends FlatSpec with Matchers {
  val l = new Staging()

  "BlockPlaceEvent()" should "sendMessage to the player" in {
    // TODO
    val result = 1
    result.shouldBe(1)
  }
}
